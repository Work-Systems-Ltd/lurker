[from-pbx]
;; Receive call from PBX, route into Stasis for RTP interception
exten => alice,1,Stasis(lurker,dialed=alice)
 same => n,Hangup()

exten => bob,1,Stasis(lurker,dialed=bob)
 same => n,Hangup()
