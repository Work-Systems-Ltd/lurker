[from-pbx]
;; Receive call from PBX, route into Stasis for RTP interception
;; The ARI controller will handle snooping and forwarding
exten => _X.,1,NoOp(Proxy received call for ${EXTEN} from PBX)
 same => n,Stasis(lurker,dialed=${EXTEN})
 same => n,Hangup()
